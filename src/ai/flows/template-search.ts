'use server';

/**
 * Placeholder Template Search Flow
 * -------------------------------
 * This minimal file exists to satisfy the import in `src/ai/dev.ts` and to
 * keep Genkit from crashing ("Cannot find module '@/ai/flows/template-search.ts'").
 *
 * You can replace this stub with a real implementation that searches template
 * documents and returns relevant results. For now, we simply export an empty
 * async function so Genkit registers a no-op flow.
 */

export async function templateSearch() {
  return { message: 'Template search is not implemented yet.' };
} 